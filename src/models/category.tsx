class Category {
  id: string;
  title: string;
  color: string;
  constructor(id: string, title: string, color) {
    this.id = id;
    this.title = title;
    this.color = color;
  }
}

export default Category;
